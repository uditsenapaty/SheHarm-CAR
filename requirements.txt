# SheHarm-CAR — pinned to what this environment was verified with.
# Install:  pip install -r requirements.txt

# --- core ---
torch>=2.4                 # verified on 2.8.0+cu128
transformers>=4.45         # verified on 5.14.1 (CLIPVisionModel exposes submodules directly)
accelerate>=0.34
sentencepiece>=0.1.99
safetensors>=0.4

# --- data ---
pandas>=2.0
numpy>=1.24
pillow>=10.0
pyyaml>=6.0
scikit-learn>=1.3

# --- rationale evaluation ---
bert-score>=0.3.13         # optional: sheharm/metrics.py falls back to a built-in implementation

# --- annotation / OCR passes (GPU, 4-bit Qwen2.5-VL) ---
bitsandbytes>=0.43         # verified on 0.50.0
qwen-vl-utils>=0.0.8
python-dotenv>=1.0

# --- prompted VLM baselines ---
# LLaVA / InternVL / Llama-3.2-Vision load through transformers' AutoModelForImageTextToText.
# Llama-3.2-Vision is gated: accept the licence on the Hub and export HF_TOKEN.
timm>=1.0                  # required by some InternVL checkpoints
einops>=0.8
